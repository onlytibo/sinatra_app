<html>
  <head>
    <title>The Gossip Project</title>
  </head>
  <body>
    <h1>Mon super site de gossip, depuis le dossier views !</h1>

    <a href="/gossips/new/">Ajouter une Gossip</a>
    <ul>
      <% gossips.each_with_index do |gossip, index| %>
        <p>
          <%= id = index+1 %>
          <a href="/gossip/<%=id%>"><strong><%= gossip.content%><br /></strong></a>
          <span style="font-style:italic">Posté à <%= gossip.date%> par <%= gossip.author%></span>
        </p>
      <% end %>
    </ul>
  </body>
</html>

